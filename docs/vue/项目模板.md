# 项目模板

## 文件导出

```js
// 导出api
export function apiExport(form) {
  return request({
    url: 'xxxxxx',
    method: 'post',
    data: form,
    responseType: form.isExport === 1 ? 'blob' : ''
  })
}
```
